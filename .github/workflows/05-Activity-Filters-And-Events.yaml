name: 05-Activity-Filters-And-Events

on:
  pull_request:
    types:
      - closed

    branches:
      - master

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running whenever a PR is opened or synchronize and base branch is main"